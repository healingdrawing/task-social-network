<template>
  <pre>
    //todo: add implementation
    BellView.vue displays the list of notifications for the user.
    There are x4 types of notifications:
    1. "event" - when an event is created in a group the user is a member of. No action required.
    2. "following" - when other user sends a follow request to the user , and user profile is private. Accept or reject required.
    3. "invitation" - when other user invites the user to a group(include case of invitation from group creator in time of group creation). Accept or reject required.
    4. "request" - when other user sends a request to join a group the user is a creator of. Accept or reject required.
    wip... continue next session. about buttons and actions.
  </pre>
</template>